# IPbus Simulation

This is a simulation of the full IPbus system, including Verilog-Ethernet integration. It uses the RMII interface.

The design includes example slave modules used as payload.

To run the simulation:
```bash
make simulate 
```