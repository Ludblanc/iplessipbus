# Verilog-Ethernet Simulation

This is a simulation of Verilog-Ethernet isolated, that uses the RMII interface.

It receives and decodes Ethernet/IP/UDP packets, then echoes them back to verify correct parsing and transmission.

To run the simulation:
```bash
make simulate 
```